{
  "dependencies": {
    "express": "^4.18.1",
    "node.js": "^0.0.1-security",
    "socket.io": "^4.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "start": "nodemon server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
